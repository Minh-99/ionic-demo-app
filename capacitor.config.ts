import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'deploy-ionic-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
